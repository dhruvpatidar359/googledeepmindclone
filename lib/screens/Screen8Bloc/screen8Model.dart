// this contains the list of the videos and text for the widget of screen8

class Screen8Model {
  String number;
  String name;

  int seekPosition;
  Screen8Model(this.number, this.name, this.seekPosition);
}
